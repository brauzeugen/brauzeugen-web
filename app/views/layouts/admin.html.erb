<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Brauzeugen</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'admin', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'admin', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <% flash.each do |key, value| %>
        <div class="<%= flash_class(key) %>">
          <%= value %>
        </div>
      <% end %>

      <header>
        <ul class="list-inline">
          <li class="list-inline-item"><%= link_to 'Dashboard', admin_path %></li>
          <li class="list-inline-item"><%= link_to 'Users', admin_users_path %></li>
          <li class="list-inline-item"><%= link_to 'Voices', admin_voices_path %></li>
          <li class="list-inline-item"><%= link_to 'Brew batches', admin_brew_batches_path %></li>
        </ul>
      </header>

      <%= yield %>
    </div>
  </body>
</html>
